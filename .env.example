# Public URL for the locally running Astronomy Shop.
# Checkly's public runners cannot reach localhost, so either tunnel it
# (cloudflared / ngrok) or use a Checkly Private Location.
ENVIRONMENT_URL=https://your-tunnel.trycloudflare.com
